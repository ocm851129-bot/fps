# TRIAD: LAST CIRCLE — 게임 디자인 문서

작성일: 2026-09-12 · 버전 0.1

## 1. 게임 개요

탑다운 생존 슈터 / 배틀로얄. 정사각형 1000 × 1000 월드에서 자원을 확보하고 마지막까지 살아남는다. 간단한 원형 캐릭터, 색으로 구분되는 아이템, 명확한 안전 지역을 사용한다. 기존 게임의 명칭·맵·아트 자산을 복제하지 않는 독립 모드다.

현재 HTML5 버전은 사용자 1명 + AI 99명의 로컬 시뮬레이션이다. 실제 100인 온라인 매치, 계정, 결제, 서버 순위표는 포함하지 않는다. 기존 TRIAD 3인 온라인 FPS와 별도 실행된다. PC 브라우저를 기본으로 터치 이동·조준·발사를 지원한다. 타겟은 짧은 경기와 쉬운 조작을 선호하는 슈터 이용자다.

## 2. 핵심 게임플레이

출격 → 5초 낙하 연출 → 착지 → 무기 탐색 → 전투·회복 → 안전 지역 이동 → 최종 생존. 현 버전은 충돌 없는 위치에 자동 배치되며, 비행 경로 선택과 수동 낙하는 향후 기능이다. 시작 시 무기는 없고, 사용자 주변에 권총 한 개가 배치된다. AI에도 동일한 수집 규칙을 적용한다.

- 이동: WASD, 대각선 속도 정규화. 월드 경계와 사각형 건물에 충돌한다.
- 조준·사격: 마우스 방향 / 클릭 유지. 탄환은 벽에 막히며 소구간 이동으로 관통 오류를 줄인다.
- 수집: 18m 이내 자동 수집. 현재는 사거리가 더 긴 무기로 자동 교체한다. 수동 슬롯 교체는 후속 기능이다.
- 재장전: R, 빈 탄창은 자동 재장전. 예비 탄약이 있어야 한다.
- 회복: F, 키트 1개로 즉시 HP 40 회복. 전투 중 사용 가능하다.
- 사망: 부활 없음. 자신의 등수·처치 수 표시 후 AI 경기를 관전하거나 재시작한다.
- 승리: 살아 있는 참가자가 1명일 때 종료. 같은 시뮬레이션 틱에 모두 사망하면 생존자 없음으로 종료한다.

### 안전 지역

현재 중심은 (500, 500)에 고정된다. 초기 반경 710m로 맵 전체를 포함한다. 경기 시작 35초부터 275초까지 반경을 710m에서 0m까지 선형 축소한다. 속도는 약 2.96m/초다. 밖에 있으면 초당 2 HP부터 시작하여 40초마다 2씩 피해가 증가한다. 방어구는 현 구현에서 안전 지역 피해에도 40% 흡수를 적용한다. 상용 버전은 자기장 피해의 방어구 무시를 별도 밸런스 실험할 수 있다.

현재 UI는 푸른 외곽 영역과 경계선, 남은 반경, 미니맵을 보여준다. 다음 온라인 버전에서는 내부에 완전히 포함되는 무작위 다음 원을 미리 표시하고 대기/이동 단계, 경고음, 도달 예상 시간을 추가한다. 무작위 원이 막힌 지형에 놓이지 않도록 이동 가능 지형 검사를 한다.

### 무기·아이템 밸런싱

| 무기 | 피해/발 | 발사 간격 | 탄창 | 재장전 | 사거리 | 역할 |
|---|---:|---:|---:|---:|---:|---|
| P9 | 22 | 0.36초 | 12 | 1.4초 | 180m | 초반 자위 |
| VECTOR | 14 | 0.11초 | 30 | 1.8초 | 145m | 근거리 연사 |
| AR-30 | 27 | 0.22초 | 30 | 2.1초 | 260m | 중거리 범용 |

피해는 거리 감쇠 없는 초기 실험값이다. 원형 충돌체이므로 헤드샷은 없다. 탄환 속도는 360m/초다. AI 조준 오차는 ±0.12라디안으로 두고, 약 170m 이내 적만 탐지한다. AI는 0.25~0.55초 간격으로 목표를 갱신한다. 엄폐물 뒤에도 발사할 수 있지만 탄환은 벽에 차단된다. 고급 엄폐 판단과 길찾기는 후속 단계다.

초기 무기 180개, 탄약 80개, 치료 45개, 방어구 35개와 사용자 주변 권총 1개가 생성된다. 무기를 주우면 해당 탄창과 예비탄 60발을 받는다. 탄약 상자는 30발이며 상자 수집 시 예비탄 최대 180발, 키트 최대 3개다. 방어구는 내구도 50, 피해의 40% 흡수다. 사망자는 탄약 한 개를 남긴다. 킬 보상으로 능력치를 올리지 않는다.

## 3. 진행 시스템

| 경기 시간 | 상태 | 반경 / 목표 |
|---|---|---|
| 0~5초 | 자동 낙하 연출 | 710m, 이동·전투 대기 |
| 5~35초 | 착지·파밍 | 710m 유지 |
| 35~115초 | 초반 생존 | 710 → 약 473m |
| 115~195초 | 중반 교전 | 약 473 → 237m |
| 195~275초 | 최종 전투 | 약 237 → 0m |
| 275초 이후 | 종료 유도 | 외곽 피해 지속, 1명 이하에서 종료 |

화면이 비활성화되거나 사용자가 일시정지하면 로컬 시뮬레이션도 멈춘다. 온라인 설계에서는 서버 시간이 기준이며 개인 일시정지는 허용하지 않는다. 진행·보상은 현 모드에서 저장하지 않으며 기존 FPS 계급과도 별도다.

## 4. UI / UX

메인 로비 → 낙하 시작 → 전투 HUD → 사망 결과 / 최종 우승 → 관전 또는 재출격. FPS 로비로 돌아가는 링크와 문서 링크를 제공한다.

HUD: 생존 인원, 경기 시간, 안전 반경, 처치 수, HP, 방어구, 무기, 탄창/예비탄, 치료 키트, 재장전 상태. 미니맵은 자기 위치와 현재 원을 표시한다. 탑다운 전체 맵을 보는 공개 정보형 프로토타입이므로 상대 위치도 화면에 보인다. 상용판은 카메라 추적과 시야 제한을 검토한다.

사용자는 연두색과 YOU 라벨, 적은 주황색, 무기는 노란색, 치료는 분홍색, 기타 물자는 파란색이다. 모바일은 왼쪽 패드 이동 + 전장 터치 조준·사격, 치료/재장전 버튼을 제공한다. 현재 전체 맵 표시 특성상 작은 화면에서는 정밀 조준에 한계가 있다. 후속 UX는 카메라 확대, 별도 조준 스틱, 아이템 아이콘, 접근성 색상 설정을 우선한다.

## 5. 기술 아키텍처

### 현재 실행 구조

royale-core.js: DOM 없는 시뮬레이션, 생성·AI·물리·전투·안전 지역·종료 처리.
royale.js: 입력, Canvas 렌더링, HUD, 터치 조작.
royale.html / royale.css: 화면 구조와 반응형 UI.
Vercel: 정적 파일 제공. 외부 서비스나 신규 비용 없이 실행한다. 최대 프레임 간격은 50ms로 제한되어 느린 기기에서는 실시간보다 경기가 느려질 수 있다. 현재는 로컬 가변 스텝이며, 서버용 고정 스텝은 별도 구현한다.

### 실제 100인 온라인 확장안

브라우저/엔진 클라이언트 → 인증·매치메이킹 API → 매치 할당 → 지역별 전용 게임 서버 → 결과 검증 → 영속 저장소. 인증/스킨/통계와 실시간 전투를 분리한다. Vercel 정적 호스팅이나 기존 Supabase Broadcast 방을 100인 전투 서버로 간주하지 않는다.

게임 서버가 위치, 총알, 재장전, 탄약, 피해, 루팅, 안전 지역과 승패의 유일한 권한을 가진다. 클라이언트는 입력 순번과 틱을 보내고 결과를 임의 확정하지 않는다. 한 매치는 단일 권한 시뮬레이션 루프가 처리하여 동시 아이템 획득과 중복 피해를 직렬화한다. 여러 매치는 여러 프로세스로 확장한다. 계정/결과 저장은 경기 ID로 중복 삽입을 막는다.

초기 목표: 서버 30Hz, 스냅샷 10~20Hz, 입력 30Hz. 로컬 예측·서버 교정, 원격 보간 100ms 내외를 시작점으로 측정한다. 사격 지연 보정은 최근 200ms 정도의 충돌체 이력을 되감되 서버가 허용한 시간만 인정한다. 발사 속도·속도 초과·탄약·시야와 입력 시퀀스를 검증한다.

공간 그리드 / 관심 영역(AOI)로 가까운 개체만 전송하고, 멀리 있는 개체는 낮은 빈도 또는 제외한다. 상태 델타, 좌표 양자화, 휴면 아이템, 중요 이벤트의 신뢰 전송과 이동 스냅샷의 최신값 우선 정책을 사용한다. 브라우저 전송 방식은 지원 기기 테스트 후 WebSocket / WebTransport를 선택한다. 100인 × 전체 개체 매 틱 전송은 피한다.

Unity의 Netcode for Entities는 서버 권한 및 예측 프레임워크를 제공한다: [Unity 공식 네트코드 비교](https://docs.unity.com/multiplayer/netcode/netcode). Unreal은 대량 Actor의 복제를 위한 Replication Graph를 제공한다: [Epic 공식 Replication Graph](https://dev.epicgames.com/documentation/unreal-engine/replication-graph-in-unreal-engine). 엔진 기능만으로 100인 성능이 보장되지는 않으며 목표 맵에서 부하 검증해야 한다.

## 6. 수익 모델 제안

현재 구현은 무료이며 결제 기능이 없다. 상용화 시 캐릭터·무기 외형, 낙하산, 감정 표현과 시즌 배틀패스를 제안한다. 유료 아이템이 피해·체력·가시성·매칭 우위를 제공하지 않도록 한다. 유료 랜덤 상자보다 가격이 명확한 직접 구매를 우선하고 기본 전투 장비는 전원에게 동일하게 제공한다.

시즌 패스는 8~12주 콘텐츠 공급 역량을 확보한 뒤 도입한다. 완료 가능 시간, 이탈률, 구매 전환, 환불·고객 지원 비용을 함께 측정한다. 예상 매출은 현재 사용자 지표가 없어 산정하지 않는다. 판매 국가의 결제·개인정보·청소년 정책은 출시 전에 별도 검토한다.

## 검증과 후속 우선순위

생성 인원 100, 접근 가능한 스폰, 충돌, 피해와 탄약, 안전 반경 단조 감소, 최종 종료를 자동 검증한다. UI에서는 PC 조작, 터치 취소, 초점 이탈, 재시작을 확인한다. 다음 단계는 공간 해시로 총알/적 탐색 최적화 → AI 길찾기 → 16인 전용 서버 시험 → 32/64/100인 부하 검증 순서다.
