{
  "name": "triad-arena",
  "version": "1.0.0",
  "private": true,
  "description": "Three-operator mobile and desktop arena FPS",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
